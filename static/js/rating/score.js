// $(document).ready(function() {
//     alert('Hello world!')
// })
$(document).ready(function() {


    // $("#months").append('<ul id="months-container"></ul>')
    // //Чтобы это работало нужно создать глобальную переменную window.months = {{ months|safe }}; в отдельном скрипте (на самой странице), далее записать в нее данные из питона ;
    // for (month in months) {
    //     month_pattern = '<li>' + months[month] + '</li>';
    //     $("#months-container").append(month_pattern);
    // }


    // for (month in "{{ months }}") {
    //     $("#months-container").append(month)
    // }


    // for (var month = 0, month < month.length; month++) {
    //     $("#months-container").append()
    // }


});
